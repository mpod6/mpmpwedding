# yaml-language-server: $schema=./quartz/plugins/quartz-plugins.sche json
configuration:
  pageTitle: Marisa & Max -- August 28, 2027
  pageTitleSuffix: " | Marisa & Max's Wedding"
  enableSPA: true
  enablePopovers: false
  analytics:
    provider: plausible
  locale: en-US
  baseUrl: mpmp.wedding
  ignorePatterns:
    - private
    - templates
    - .obsidian
  theme:
    fontOrigin: googleFonts
    cdnCaching: true
    typography:
      header: Cormorant Garamond  # elegant high-contrast serif, italic for names
      body: EB Garamond           # romantic, readable body serif
      code: JetBrains Mono        # monospace
    colors:
      lightMode:
        light:         "#f7f3e8"                   # page bg — soft ivory (Vinery paper)
        lightgray:     "#e0d9c4"                   # borders — warm stone (venue greige)
        gray:          "#a89f83"                   # muted details, small caps accents
        darkgray:      "#45483c"                   # body text — warm deep olive-charcoal
        dark:          "#2e3527"                   # headers — deep forest charcoal
        secondary:     "#5f7247"                   # links & accents — olive sage vine
        tertiary:      "#7d8f58"                   # hover — lighter moss
        highlight:     "rgba(125, 143, 88, 0.12)"  # internal link bg
        textHighlight: "rgba(196, 181, 110, 0.35)" # ==markdown highlight== bg
      darkMode:        # intentionally identical — one consistent stationery look
        light:         "#f7f3e8"
        lightgray:     "#e0d9c4"
        gray:          "#a89f83"
        darkgray:      "#45483c"
        dark:          "#2e3527"
        secondary:     "#5f7247"
        tertiary:      "#7d8f58"
        highlight:     "rgba(125, 143, 88, 0.12)"
        textHighlight: "rgba(196, 181, 110, 0.35)"
plugins:
  - source: github:quartz-community/created-modified-date
    enabled: true
    options:
      defaultDateType: modified
      priority:
        - frontmatter
        - git
        - filesystem
    order: 10
  - source: github:quartz-community/syntax-highlighting
    enabled: true
    options:
      theme:
        light: github-light
        dark: github-dark
      keepBackground: false
    order: 20
  - source: github:quartz-community/obsidian-flavored-markdown
    enabled: true
    options:
      comments: true
      highlight: true
      wikilinks: true
      callouts: true
      mermaid: true
      parseTags: true
      parseArrows: true
      parseBlockReferences: true
      enableInHtmlEmbed: false
      enableYouTubeEmbed: true
      enableVideoEmbed: true
      enableCheckbox: true
    order: 30
  - source: github:quartz-community/github-flavored-markdown
    enabled: true
    order: 40
  - source: github:quartz-community/table-of-contents
    enabled: false
    order: 50
    layout:
      position: right
      priority: 30
  - source: github:quartz-community/crawl-links
    enabled: true
    options:
      markdownLinkResolution: shortest
    order: 60
  - source: github:quartz-community/description
    enabled: true
    order: 70
  - source: github:quartz-community/latex
    enabled: true
    options:
      renderEngine: katex
    order: 80
  - source: github:quartz-community/citations
    enabled: false
    order: 85
  - source: github:quartz-community/hard-line-breaks
    enabled: true
    order: 90
  - source: github:quartz-community/ox-hugo
    enabled: false
    order: 91
  - source: github:quartz-community/roam
    enabled: false
    order: 92
  - source: github:quartz-community/remove-draft
    enabled: true
  - source: github:quartz-community/explicit-publish
    enabled: false
  - source: github:quartz-community/unlisted-pages
    enabled: true
    options: {}
    order: 45
  - source: github:quartz-community/encrypted-pages
    enabled: false
    options:
      iterations: 600000
      passwordField: password
      unlistWhenEncrypted: false
      outputPath: static/encryptedContentIndex.json
  - source: github:quartz-community/stacked-pages
    enabled: false
    layout:
      position: afterBody
      priority: 50
      display: all
  - source: github:quartz-community/alias-redirects
    enabled: true
  - source: github:quartz-community/content-index
    enabled: true
    options:
      enableSiteMap: true
      enableRSS: false
  - source: github:quartz-community/favicon
    enabled: true
  - source: github:quartz-community/og-image
    enabled: false # replaced by a hand-designed static preview at
                   # quartz/static/og-image.png (Quartz falls back to it
                   # automatically); per-page generated previews added nothing
                   # for a 9-page wedding site
  - source: github:quartz-community/cname
    enabled: true
  - source: github:quartz-community/canvas-page
    enabled: true
  - source: github:quartz-community/content-page
    enabled: true
  - source: github:quartz-community/folder-page
    enabled: true
  - source: github:quartz-community/tag-page
    enabled: true
  - source: github:quartz-community/explorer
    enabled: false # replaced by ./plugins/wedding-nav static top nav
    options:
      sortFn: |
        (a, b) => {
          if (a.isFolder && !b.isFolder) return -1
          if (!a.isFolder && b.isFolder) return 1
          const getOrder = (node) => {
            const tags = (node.data && node.data.tags) || []
            const navTag = tags.find(t => /^nav-\d+$/.test(t))
            return navTag ? parseInt(navTag.split("-")[1], 10) : null
          }
          const orderA = getOrder(a)
          const orderB = getOrder(b)
          if (orderA !== null && orderB !== null) return orderA - orderB
          if (orderA !== null) return -1
          if (orderB !== null) return 1
          return (a.displayName || "").localeCompare(b.displayName || "", undefined, { numeric: true, sensitivity: "base" })
        }
    layout:
      position: left
      priority: 50
  - source: github:quartz-community/graph
    enabled: false
    layout:
      position: right
      priority: 10
  - source: github:quartz-community/search
    enabled: false # 9-page site; nav covers everything
    layout:
      position: left
      priority: 20
      group: toolbar
      groupOptions:
        grow: true
  - source: github:quartz-community/backlinks
    enabled: false
    layout:
      position: right
      priority: 50
  - source: github:quartz-community/article-title
    enabled: true
    layout:
      position: beforeBody
      priority: 10
  - source: github:quartz-community/content-meta
    enabled: false
    layout:
      position: beforeBody
      priority: 20
  - source: github:quartz-community/tag-list
    enabled: false
    layout:
      position: beforeBody
      priority: 30
  - source: github:quartz-community/page-title
    enabled: false # replaced by wedding-nav masthead wordmark
    layout:
      position: left
      priority: 10
  - source: github:quartz-community/darkmode
    enabled: false # single stationery appearance; no theme toggle
    layout:
      position: left
      priority: 30
      group: toolbar
  - source: github:quartz-community/reader-mode
    enabled: false
    layout:
      position: left
      priority: 35
      group: toolbar
  - source: github:quartz-community/breadcrumbs
    enabled: false
    layout:
      position: beforeBody
      priority: 5
      condition: not-index
  - source: github:quartz-community/comments
    enabled: false
    options:
      provider: giscus
      options: {}
    layout:
      position: afterBody
      priority: 10
  - source: github:quartz-community/footer
    enabled: true
    options:
      links:
        RSVP: /rsvp
        Travel & Hotel Information: /travel
        Registry: /registry
  - source: github:quartz-community/recent-notes
    enabled: false
  - source: github:quartz-community/spacer
    enabled: false
    options: {}
    order: 25
    layout:
      position: left
      priority: 25
      display: mobile-only
  - source: github:quartz-community/bases-page
    enabled: true
    options: {}
    order: 50
  - source: github:quartz-community/note-properties
    enabled: true
    options:
      includeAll: false
      includedProperties:
        - description
        - aliases
      excludedProperties: []
      hidePropertiesView: false
      delimiters: ---
      language: yaml
    order: 5
    layout:
      position: beforeBody
      priority: 15
      display: all
  - source:
      name: quartz-themes
      repo: github:saberzero1/quartz-themes
      subdir: plugin
    enabled: false # DISABLED: this plugin's Obsidian theme CSS loads after the
                   # config-generated stylesheet and overrides the color palette
                   # defined above (this was the "colors don't apply" bug)
    options:
      theme: default
  - source: github:quartz-community/obsidian-plugin-excalidraw
    enabled: true
    options:
      enableInteraction: true
      darkMode: auto
      exportPadding: 20
    order: 50
  - source: ./plugins/wedding-nav
    enabled: true
    layout:
      position: beforeBody
      priority: 1 # renders above article-title (priority 10) on every page
  - source: ./plugins/rsvp-widget
    enabled: true
    options:
      # ── THE GATE ──────────────────────────────────────────────────────────
      # false → the RSVP page shows "RSVPs will open at a later date"
      # true  → the interactive RSVP form is live
      # (Optional backstop: set Netlify env var RSVP_OPEN=false to also block
      #  the API itself while the gate is down.)
      rsvpOpen: false
      # Meal choices shown in the form — update once the menu is set with the venue
      meals:
        - Herb-Roasted Chicken
        - Pan-Seared Salmon
        - Vegetarian
    layout:
      position: afterBody
      priority: 10 # renders beneath the page text; component only outputs on /rsvp
layout:
  groups:
    toolbar:
      priority: 35
      direction: row
      gap: 0.5rem
  byPageType:
    "404":
      template: full-width
      positions:
        left: []
        right: []
    content:
      template: full-width
    folder:
      template: full-width
      exclude:
        - reader-mode
      positions:
        right: []
    tag:
      template: full-width
      exclude:
        - reader-mode
      positions:
        right: []
    canvas: {}
    bases: {}
